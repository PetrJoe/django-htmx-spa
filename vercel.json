{
    "builds": [{
        "src": "djangohtmxspa/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "djangohtmxspa/wsgi.py"
        }
    ]
}
